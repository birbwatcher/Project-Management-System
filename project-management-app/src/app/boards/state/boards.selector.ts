import { createSelector, createFeatureSelector } from '@ngrx/store';
import { Board, State } from './boards.state';

/* export const selectBoards = createSelector(
    (state: State) => state.boards,
    (boards: Board[]) => boards
  ); */
